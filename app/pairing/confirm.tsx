import { Text, View } from "react-native";

// Phase 3 stub. Waits for the device to report online, then POSTs the
// claim code to /v1/devices/{id}/claim.
export default function ConfirmScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-white px-6">
      <Text className="text-lg">Pairing → confirm — phase 3 stub</Text>
    </View>
  );
}
