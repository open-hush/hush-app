import { Text, View } from "react-native";

// Phase 3 stub. Collects SSID + password and sends over BLE (Improv WiFi).
export default function WifiScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-white px-6">
      <Text className="text-lg">Pairing → WiFi credentials — phase 3 stub</Text>
    </View>
  );
}
